# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'FinalGame' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
	pod ‘Mixpanel’
  # Pods for FinalGame

  target 'FinalGameTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FinalGameUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
